
# Project settings
PROJECT_NAME=drf-template

# Web server settings
WEB_HOST=127.0.0.1
DEV_PORT=8001
TEST_PORT=8002

# Django Configuration
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1
SECRET_KEY=your-secret-key-here-change-in-production
DJANGO_SETTINGS_MODULE=config.settings
DATABASE_CONN_MAX_AGE=0

# Database settings
# USE_SQLITE=true   # Use SQLite for local/test (default: true for fast development)
# USE_SQLITE=false  # Use PostgreSQL for docker/production (required for containers)
# DATABASE_URL=postgresql://user:pass@host:5432/dbname  # Required when USE_SQLITE=false

POSTGRES_IMAGE=postgres:16-alpine
POSTGRES_USER=polls_user
POSTGRES_PASSWORD=polls_password
POSTGRES_HOST_DB_NAME=polls_site
POSTGRES_DEV_DB_NAME=polls_site_dev
POSTGRES_TEST_DB_NAME=polls_site_test